---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "koomgr.service" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "koomgr.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: auth-server
      port: {{ .Values.auth.servicePort }}
      targetPort: {{ .Values.auth.targetPort }}
      protocol: TCP
    - name: webhook-server
      port: {{ .Values.webhooks.servicePort }}
      targetPort: {{ .Values.webhooks.targetPort }}
      protocol: TCP
  selector:
    {{- include "koomgr.selectorLabels" . | nindent 4 }}
